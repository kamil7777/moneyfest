/* eslint-disable linebreak-style */

/* import Figure from './module/figure'; */

/* const square = new Figure(50, 50);
const rectangle = new Figure(100, 50);
for (let i = 1; i < 10; i += 1) {
  square.createElm('constructor');
}
for (let i = 1; i < 10; i += 1) {
  rectangle.createElm('contr');
}

const elm = document.querySelectorAll('.constructor');
const cont = document.querySelector('.container');
cont.addEventListener('click', () => {
  elm.forEach((item) => item.classList.toggle('active'));
}); */
